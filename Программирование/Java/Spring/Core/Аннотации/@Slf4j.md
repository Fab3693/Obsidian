### 🧾 **@Slf4j**

**Простыми словами:** `@Slf4j` — это аннотация из библиотеки Lombok, которая **автоматически добавляет в класс объект логгера** для записи логов с помощью [SLF4J](https://www.slf4j.org/).

---

### ⚙️ **Когда использовать**

- Чтобы не писать вручную код создания логгера:
    

```java
private static final Logger log = LoggerFactory.getLogger(MyClass.class);
```

- Для удобного логирования событий, ошибок и отладочной информации
    

---

### 📌 **Пример использования**

```java
[[ @Slf4j ]]
public class MyService {

    public void doWork() {
        log.info("Работа началась");
        try {
            // какая-то логика
        } catch (Exception e) {
            log.error("Ошибка в работе", e);
        }
    }
}
```

---

### 🧠 **Особенности**

- Требует Lombok в проекте
    
- Работает с разными реализациями логгирования через SLF4J (Logback, Log4j и др.)
    
- Упрощает поддержку и читабельность кода
    

---

### 🔗 **Связанные термины**

- [[Lombok]]
    
- [[SLF4J]]
    
- [[Logback]]
    
- [[Log4j]]
    

---

#java #logging #lombok #slf4j #spring